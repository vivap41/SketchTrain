<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SketchTrain"
        tools:targetApi="31">
        <activity
            android:name=".login.SignInActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.SignUpActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".ui.newusers.SideMenu"
            android:exported="true">
        </activity>
        <activity
            android:name=".ui.newusers.strength.SecondStep"
            android:exported="true">
        </activity>
        <activity
            android:name=".ui.newusers.strength.hiper.ThirdStep"
            android:exported="true">
        </activity>
        <activity
            android:name=".ui.newusers.strength.hiper.routine.FourthStep"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
        </activity>
    </application>

</manifest>